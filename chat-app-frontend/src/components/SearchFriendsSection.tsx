import {Paper, Typography} from "@mui/material";
import React from "react";

export const SearchFriendsSection = () => {
    return (

        <Paper elevation={3}>
            <Typography variant={"h4"} sx={{px: 2, py: 1}}>Search</Typography>
        </Paper>
    );
};